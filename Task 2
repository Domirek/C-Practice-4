#include <stdio.h>

int main() {
  int blue_balls[5];
  int size = sizeof(blue_balls) / sizeof(blue_balls[0]);
  printf("In box blue_balls fits %d balls.\n", size);
  return 0;
}

//Вывод:
//In box blue_balls fits 5 balls.
